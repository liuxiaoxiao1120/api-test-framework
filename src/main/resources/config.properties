loginBaseUrl=http://10.6.20.233:8891
# Credentials: set LOGIN_USER / LOGIN_PASSWORD, or put loginPath (and optional URLs) in classpath config.local.properties (see .gitignore).
loginPath=/backend/zlt/teammate/login?userName=${env:LOGIN_USER}&password=${env:LOGIN_PASSWORD}
apiBaseUrl=http://10.6.20.233:9050
# Optional: classpath path to global header template JSON
# global.headers.file=headers/global_headers.json
